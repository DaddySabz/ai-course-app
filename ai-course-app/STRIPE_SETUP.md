I notice there's a mismatch here. The document is a comprehensive Stripe Payment Integration setup guide for a Next.js app, but the suggested code is a PowerShell command to check if Node.js is installed.

That PowerShell command doesn't belong in this guide. However, if you want to **add a prerequisite check section** to help users verify their environment, here's how I'd incorporate it:
---

## 🔧 Prerequisites Check

Before starting, verify your environment has the required tools:

### Check Node.js Installation:

**Windows (PowerShell):**

